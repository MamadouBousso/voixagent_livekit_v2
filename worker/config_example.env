# ============================================================
# CONFIGURATION EXEMPLE POUR L'AGENT MODULAIRE
# ============================================================

# ============================================================
# LIVEKIT CONFIGURATION
# ============================================================
LIVEKIT_URL=wss://your-livekit-server.com
LIVEKIT_API_KEY=your-api-key
LIVEKIT_API_SECRET=your-api-secret

# ============================================================
# CONFIGURATION LLM (Large Language Model)
# ============================================================
LLM_PROVIDER=openai                    # Provider: openai, anthropic, cohere (après ajout)
LLM_MODEL=gpt-4o-mini                  # Modèle: gpt-4o-mini, gpt-4o, claude-3-sonnet, etc.
LLM_MAX_TOKENS=1000                    # Nombre maximum de tokens de réponse
LLM_TEMPERATURE=0.7                    # Créativité (0.0 = déterministe, 1.0 = très créatif)
OPENAI_API_KEY=your-openai-api-key     # Clé API OpenAI

# ============================================================
# CONFIGURATION STT (Speech-to-Text)
# ============================================================
STT_PROVIDER=openai                    # Provider: openai, google, azure (après ajout)
STT_MODEL=whisper-1                    # Modèle: whisper-1, etc.

# ============================================================
# CONFIGURATION TTS (Text-to-Speech)
# ============================================================
TTS_PROVIDER=openai                    # Provider: openai, elevenlabs, azure (après ajout)
TTS_MODEL=tts-1                        # Modèle: tts-1, etc.
TTS_VOICE_ID=alloy                     # Voix: alloy, echo, fable, onyx, nova, shimmer

# ============================================================
# CONFIGURATION VAD (Voice Activity Detection)
# ============================================================
VAD_PROVIDER=silero                    # Provider: silero

# ============================================================
# CONFIGURATION AGENT
# ============================================================
AGENT_INSTRUCTIONS=You are a friendly, concise assistant. Keep answers short and helpful.

# Options avancées
ENABLE_BARGE_IN=false                  # Permet à l'utilisateur d'interrompre l'agent
MIN_ENDPOINTING_DELAY=0.5              # Délai minimum avant de considérer que l'utilisateur a fini de parler (secondes)
MAX_RESPONSE_TIME=30.0                 # Temps maximum pour générer une réponse (secondes)

# ============================================================
# PLUGINS ACTIVÉS
# ============================================================
# Liste des plugins séparés par des virgules
# Exemples: example, sentiment_analysis, profanity_filter, conversation_memory
ENABLED_PLUGINS=example

# ============================================================
# MÉTRIQUES ET MONITORING
# ============================================================
ENABLE_METRICS=true                    # Activer la collecte de métriques
METRICS_RETENTION_HOURS=24             # Durée de rétention des métriques (heures)

# ============================================================
# OPTIONS DE DÉVELOPPEMENT
# ============================================================
DEBUG=false                            # Activer les logs de debug
LOG_LEVEL=INFO                         # Niveau de log: DEBUG, INFO, WARNING, ERROR
